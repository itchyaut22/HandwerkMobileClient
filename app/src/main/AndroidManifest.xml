<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.jku.at.handwerkmobileclient" >

    <uses-sdk android:minSdkVersion="14" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar" >
        <activity
            android:name=".MainActivity_"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".activities.ServiceDetailActivity_"
            android:label="@string/title_service_detail" >
        </activity>
        <activity
            android:name=".activities.AddManufacturerActivity_"
            android:label="@string/title_add_manufacturer" >
        </activity>
        <activity
            android:name=".activities.AddServiceActivity_"
            android:label="@string/title_add_service" >
        </activity>
        <activity
            android:name=".RegisterActivity_"
            android:label="@string/title_activity_register" >
        </activity>
        <activity
            android:name=".LoginActivity_"
            android:label="@string/title_activity_login" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Used for OAuth callback -->
            <intent-filter>
                <action
                    android:name="android.intent.action.VIEW" />
                <category
                    android:name="android.intent.category.DEFAULT" />
                <category
                    android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="oauth"
                    android:host="callback" />
            </intent-filter>


        </activity>

    </application>

</manifest>
